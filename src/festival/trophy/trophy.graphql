type Trophy {
	id: Int!
	name: String!
	description: String
	classes: [FestivalClass]
}

input TrophyInput {
	name: String!
	description: String
	# classes: [FestivalClass]
}

type Query {
  trophies: [Trophy]!
  trophy(id: Int!): Trophy
}

type Mutation {
  trophyCreate(trophyInput: TrophyInput!): TrophyPayload!
  trophyUpdate(id: Int!, trophyInput: TrophyInput!): TrophyPayload!
  trophyDelete(id: Int!): TrophyPayload
}

type TrophyPayload {
  userErrors:[UserError!]!
  trophy: Trophy
}
