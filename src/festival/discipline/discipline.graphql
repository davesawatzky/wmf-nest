type Discipline {
	id: Int!
	name: String
	subdisciplines: [Subdiscipline]
}

input DisciplineInput {
	name: String!
}

type Query {
	disciplines: [Discipline]
	discipline(id: Int!): Discipline
	disciplinesByName(name: String!): [Discipline]
	disciplinesByType(SGSLabel: SGSLabel!): [Discipline]
}

type Mutation {
  disciplineCreate(disciplineInput: DisciplineInput!): DisciplinePayload
  disciplineUpdate(id: Int!, disciplineInput: DisciplineInput!): DisciplinePayload
  disciplineDelete(id: Int!): DisciplinePayload
}

type DisciplinePayload {
  userErrors: [UserError!]!
  discipline: Discipline
}
