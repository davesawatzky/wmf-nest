scalar Date
scalar DateTime
scalar Duration
scalar EmailAddress
scalar Time
scalar PhoneNumber
scalar PostalCode

type UserError {
	message: String!
	field: [String!]!
}

enum SGSlabel {
	SOLO
	GROUP
	SCHOOL
	COMMUNITY
}
